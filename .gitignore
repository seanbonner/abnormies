.DS_Store
*.swp
*.swo
.env
.env.local
node_modules/
.wrangler/
prototype.html

# Source snapshots are regenerable and throwaway (Sepolia now, mainnet later).
# Commit the derived proofs/ output instead — that is the runtime + verifiable artifact.
app/snapshot/snapshot-*.json
