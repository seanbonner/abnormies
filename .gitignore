.DS_Store
*.swp
*.swo
.env
.env.local
node_modules/
.wrangler/
prototype.html

# Test/source snapshots (Sepolia, regenerable) are throwaway and stay ignored.
# The public mainnet Phase 1 snapshot is tracked instead: holders need it to
# reproduce the committed merkle root, and the Cloudflare Pages build reads it at launch.
app/snapshot/snapshot-*.json
!app/snapshot/snapshot-25182664.json
